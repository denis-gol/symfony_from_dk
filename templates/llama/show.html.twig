{% extends 'base.html.twig' %}

{% block title %}Llama{% endblock %}

{% block body %}
    <h1>Llama</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ llama.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ llama.name }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ llama.height }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('llama_index') }}">back to list</a>

    <a href="{{ path('llama_edit', {'id': llama.id}) }}">edit</a>

    {{ include('llama/_delete_form.html.twig') }}
{% endblock %}
