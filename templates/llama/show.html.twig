{% extends 'base.html.twig' %}

{% block title %}Страница ламы{% endblock %}

{% block body %}
    <h1>Страница ламы</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ llama.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ llama.name }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ llama.height }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('llama_index') }}">Вернуться к списку</a>

    <a href="{{ path('llama_edit', {'id': llama.id}) }}">Редактировать</a>

    {{ include('llama/_delete_form.html.twig') }}
{% endblock %}
